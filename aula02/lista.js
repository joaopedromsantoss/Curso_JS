const bairros = ["Centro", "Vila Fatima", "Jaragua", "Campestre", 
    "Artemis", "Cecap", "Vale do Sol", "Paulsita", "Ondas", "Mario Dedini"]

for (let i = 0; i < bairros.length; i++) {
    const bairro = bairros[i];
    console.log(`O bairro é ${bairro}`)   
}